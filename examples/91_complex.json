{
  "user_email": "test@email.com",
  "job": {
    "company": "{{chance.word()}}",
    "phones": {
      "mobile": "{{chance.phone()}}",
      "work": "{{chance.phone()}}"
    },
    "duties": "{{chance.sentence()}}"
  },
  "personalities": {
    "favorites": {
      "number": "{{chance.d10()}}",
      "city": "{{chance.city()}}",
      "radio": "{{chance.radio()}}"
    },
    "violence-rating": "{{chance.d6()}}"
  },
  "friends" : [ "{{_$config}}", {}, {
    "name": "{{chance.name()}}",
    "phones": [ "{{_$config}}", {}, "{{chance.phone()}}"]
  }]
}
